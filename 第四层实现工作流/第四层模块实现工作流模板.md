# 第四层模块实现工作流模板

## 模块信息

### 模块名称
[模块名称]

### 所属子系统
[所属子系统名称]

### 实现优先级
[高/中/低]

### 预计实现周期
[预计实现周期，如：2-3周]

### 实现团队
[实现团队或负责人]

---

## Code阶段：代码实现

### 1. 环境准备

#### 开发环境要求
- **Python版本**：[指定Python版本]
- **核心依赖**：[列出核心依赖包]
- **数据库**：[指定数据库类型和版本]
- **其他工具**：[列出其他必要工具]

#### 项目结构
```
[模块名称]/
├── src/
│   └── [模块名称]/
│       ├── __init__.py
│       ├── core/
│       ├── models/
│       ├── services/
│       ├── utils/
│       └── api/
├── tests/
│   ├── unit/
│   ├── integration/
│   └── fixtures/
├── docs/
├── configs/
├── scripts/
├── requirements.txt
├── setup.py
├── README.md
└── .env.example
```

#### 配置管理
- **环境变量**：[列出关键环境变量]
- **配置文件**：[说明配置文件结构]
- **敏感信息**：[说明敏感信息处理方式]

### 2. 核心功能实现

#### 类设计
```python
# [模块名称]核心类设计
class [ModuleName]Service:
    """[模块名称]服务类"""
    
    def __init__(self, config: [ModuleName]Config):
        """初始化[模块名称]服务"""
        self.config = config
        self._initialize_components()
    
    def [main_method](self, input_data: [InputType]) -> [OutputType]:
        """
        [主要功能描述]
        
        Args:
            input_data: [输入数据描述]
            
        Returns:
            [OutputType]: [输出数据描述]
            
        Raises:
            [ExceptionType]: [异常情况描述]
        """
        # 实现主要功能逻辑
        pass
    
    def _initialize_components(self):
        """初始化组件"""
        # 实现组件初始化逻辑
        pass
```

#### 数据模型
```python
# 数据模型定义
from pydantic import BaseModel
from typing import List, Dict, Optional

class [InputModel](BaseModel):
    """输入数据模型"""
    [field1]: [type1]
    [field2]: [type2]
    # ...其他字段

class [OutputModel](BaseModel):
    """输出数据模型"""
    [field1]: [type1]
    [field2]: [type2]
    # ...其他字段

class [ConfigModel](BaseModel):
    """配置模型"""
    [config1]: [type1]
    [config2]: [type2]
    # ...其他配置
```

#### 异常处理
```python
# 自定义异常类
class [ModuleName]Error(Exception):
    """[模块名称]错误基类"""
    pass

class [SpecificError]([ModuleName]Error):
    """[具体错误类型]"""
    pass

# 异常处理示例
def [method_with_error_handling](self, input_data):
    try:
        # 验证输入
        if not self._validate_input(input_data):
            raise [SpecificError]("输入数据无效")
            
        # 执行主要逻辑
        return self._execute_main_logic(input_data)
        
    except [ModuleName]Error as e:
        logger.error(f"[模块名称]错误: {str(e)}")
        raise
    except Exception as e:
        logger.error(f"未知错误: {str(e)}")
        raise [ModuleName]Error(f"[模块名称]操作失败: {str(e)}")
```

### 3. 接口实现

#### RESTful API
```python
# API接口实现
from fastapi import FastAPI, HTTPException, Depends
from pydantic import BaseModel

app = FastAPI(title="[模块名称]API", version="1.0.0")

# API端点
@app.post("/api/[module_name]/[action]", response_model=[OutputModel])
async def [action_name](
    input_data: [InputModel],
    service: [ModuleName]Service = Depends(get_[module_name]_service)
):
    """[API功能描述]"""
    try:
        result = service.[main_method](input_data)
        return [OutputModel](
            [field1]=result.[field1],
            [field2]=result.[field2]
        )
    except [ModuleName]Error as e:
        raise HTTPException(status_code=400, detail=str(e))
```

#### 事件驱动接口
```python
# 事件驱动接口实现
import asyncio
from typing import Callable

class [ModuleName]EventHandler:
    """[模块名称]事件处理器"""
    
    def __init__(self, [module_name]_service: [ModuleName]Service):
        self.[module_name]_service = [module_name]_service
        self.event_bus = EventBus()
        self._register_handlers()
    
    def _register_handlers(self):
        """注册事件处理器"""
        self.event_bus.subscribe("[event_type]", self._handle_[event])
    
    async def _handle_[event](self, event_data):
        """处理[事件名称]事件"""
        # 实现事件处理逻辑
        pass
```

---

## Check阶段：测试验证

### 1. 单元测试

#### 测试结构
```
tests/
├── unit/
│   ├── test_core/
│   │   ├── test_[module_name]_service.py
│   │   └── test_[component1].py
│   ├── test_models/
│   │   └── test_[model_name].py
│   ├── test_services/
│   │   └── test_[service_name].py
│   └── test_utils/
│       └── test_[util_name].py
├── integration/
│   └── test_[integration_scenario].py
├── fixtures/
│   ├── [fixture1].json
│   └── [fixture2].json
└── conftest.py
```

#### 测试用例设计
```python
# 单元测试示例
import pytest
from unittest.mock import Mock, patch
from [module_name] import [ModuleName]Service, [InputModel], [OutputModel]

class Test[ModuleName]Service:
    """[模块名称]服务测试类"""
    
    @pytest.fixture
    def [module_name]_service(self):
        """创建[模块名称]服务实例"""
        config = Mock()
        return [ModuleName]Service(config)
    
    @pytest.fixture
    def sample_input(self):
        """创建示例输入数据"""
        return [InputModel](
            [field1]="[value1]",
            [field2]="[value2]"
        )
    
    def test_[main_method]_success(self, [module_name]_service, sample_input):
        """测试成功执行[主要方法]"""
        # 模拟依赖组件
        [module_name]_service.[dependency].[method] = Mock(return_value=[mock_result])
        
        # 执行测试
        result = [module_name]_service.[main_method](sample_input)
        
        # 验证结果
        assert isinstance(result, [OutputModel])
        assert result.[field1] == [expected_value]
    
    def test_[main_method]_with_[error_condition](self, [module_name]_service):
        """测试[错误条件]时的[主要方法]"""
        # 创建错误输入数据
        error_input = [InputModel](
            [field1]="",  # 无效值
            [field2]="[value2]"
        )
        
        # 执行测试并验证异常
        with pytest.raises([SpecificError]):
            [module_name]_service.[main_method](error_input)
    
    @patch('[module_name].logger')
    def test_[main_method]_with_unexpected_error(self, mock_logger, [module_name]_service, sample_input):
        """测试意外错误处理"""
        # 模拟意外错误
        [module_name]_service.[dependency].[method] = Mock(side_effect=Exception("意外错误"))
        
        # 执行测试并验证异常
        with pytest.raises([ModuleName]Error):
            [module_name]_service.[main_method](sample_input)
        
        # 验证日志记录
        mock_logger.error.assert_called()
```

#### 测试覆盖率目标
- **整体覆盖率**：不低于85%
- **核心功能覆盖率**：100%
- **异常处理覆盖率**：90%

### 2. 集成测试

#### 模块间集成测试
```python
# 集成测试示例
class Test[ModuleName]Integration:
    """[模块名称]集成测试"""
    
    @pytest.fixture
    def integrated_system(self):
        """创建集成系统"""
        # 初始化所有相关模块
        [dependency1]_module = Mock()
        [dependency2]_module = Mock()
        [module_name]_module = [ModuleName]Service(config)
        
        # 设置模块间依赖
        [module_name]_module.set_[dependency1]_module([dependency1]_module)
        [module_name]_module.set_[dependency2]_module([dependency2]_module)
        
        return {
            "[dependency1]": [dependency1]_module,
            "[dependency2]": [dependency2]_module,
            "[module_name]": [module_name]_module
        }
    
    def test_[dependency1]_to_[module_name]_flow(self, integrated_system):
        """测试从[依赖模块1]到[模块名称]的数据流"""
        # 模拟[依赖模块1]数据
        [dependency1]_data = {[field1]: "[value1]", [field2]: "[value2]"}
        integrated_system["[dependency1]"].get_latest_data = Mock(return_value=[dependency1]_data)
        
        # 执行[模块名称]功能
        input_data = [InputModel](
            [field1]="[value1]",
            [field2]="[value2]"
        )
        result = integrated_system["[module_name]"].[main_method](input_data)
        
        # 验证结果
        assert result is not None
        assert hasattr(result, '[expected_field]')
```

### 3. 性能测试

#### 性能测试场景
```python
# 使用locust进行性能测试
from locust import HttpUser, task, between

class [ModuleName]User(HttpUser):
    """[模块名称]性能测试用户"""
    wait_time = between(1, 3)
    
    @task(3)
    def [simple_operation](self):
        """测试简单操作性能"""
        input_data = {
            [field1]: "[value1]",
            [field2]: "[value2]"
        }
        self.client.post("/api/[module_name]/[action]", json=input_data)
    
    @task(1)
    def [complex_operation](self):
        """测试复杂操作性能"""
        input_data = {
            [field1]: "[complex_value1]",
            [field2]: "[complex_value2]"
        }
        self.client.post("/api/[module_name]/[action]", json=input_data)
```

#### 性能指标
- **响应时间**：[指定响应时间要求，如：95%请求在100ms内完成]
- **吞吐量**：[指定吞吐量要求，如：支持每秒100次请求]
- **资源使用**：[指定资源使用限制，如：CPU使用率不超过50%]

---

## Document阶段：文档编写

### 1. API文档

#### 接口文档结构
```markdown
# [模块名称] API文档

## 概述
[模块名称]API提供[功能描述]。

## 认证
[说明认证方式]

## 接口列表

### [接口名称]
**请求方式**：[GET/POST/PUT/DELETE]

**请求URL**：`/api/[module_name]/[endpoint]`

**请求参数**：
| 参数名 | 类型 | 必填 | 描述 |
|--------|------|------|------|
| [param1] | [type] | 是 | [description] |
| [param2] | [type] | 否 | [description] |

**请求示例**：
```json
{
  "[field1]": "[value1]",
  "[field2]": "[value2]"
}
```

**响应参数**：
| 参数名 | 类型 | 描述 |
|--------|------|------|
| [field1] | [type] | [description] |
| [field2] | [type] | [description] |

**响应示例**：
```json
{
  "[field1]": "[value1]",
  "[field2]": "[value2]"
}
```

**错误码**：
| 错误码 | 描述 |
|--------|------|
| [code1] | [description] |
| [code2] | [description] |
```

### 2. 开发文档

#### README.md结构
```markdown
# [模块名称]

## 概述
[模块名称]是真实婴儿AI管家系统的[功能描述]组件，负责[主要职责]。

## 功能特性
- [特性1]
- [特性2]
- [特性3]

## 安装指南

### 环境要求
- [要求1]
- [要求2]

### 安装步骤
```bash
# [安装步骤1]
# [安装步骤2]
# [安装步骤3]
```

## 使用指南

### 基本使用
```python
# [基本使用示例]
```

### 高级使用
```python
# [高级使用示例]
```

## 开发指南

### 代码结构
```
[模块名称]/
├── src/
│   └── [模块名称]/
│       ├── __init__.py
│       ├── main.py
│       ├── core/
│       ├── models/
│       ├── services/
│       ├── utils/
│       └── api/
├── tests/
├── docs/
└── configs/
```

### 运行测试
```bash
# [运行测试命令]
```

## 贡献指南

1. [贡献步骤1]
2. [贡献步骤2]
3. [贡献步骤3]

## 许可证

本项目采用 [许可证名称] 许可证。
```

### 3. 用户文档

#### 用户手册结构
```markdown
# [模块名称]用户手册

## 目录
1. [简介](#简介)
2. [快速开始](#快速开始)
3. [功能说明](#功能说明)
4. [使用场景](#使用场景)
5. [常见问题](#常见问题)
6. [故障排除](#故障排除)

## 简介
[模块名称]是真实婴儿AI管家系统的[功能描述]组件，负责[主要职责]。

## 快速开始

### 基本使用
1. [步骤1]
2. [步骤2]
3. [步骤3]

### 示例
[提供简单易懂的示例]

## 功能说明

### [功能1]
[详细说明功能1]

### [功能2]
[详细说明功能2]

## 使用场景

### 场景1：[场景名称]
[描述场景1的使用方法]

### 场景2：[场景名称]
[描述场景2的使用方法]

## 常见问题

### Q1: [问题1]
A1: [回答1]

### Q2: [问题2]
A2: [回答2]

## 故障排除

### 问题1: [问题名称]
[解决方案]

### 问题2: [问题名称]
[解决方案]
```

---

## 实施计划

### 第一周：环境准备与核心功能实现
- [ ] 搭建开发环境
- [ ] 设计项目结构
- [ ] 实现核心类和接口
- [ ] 编写基础单元测试

### 第二周：功能完善与测试
- [ ] 完善所有功能实现
- [ ] 编写完整单元测试
- [ ] 进行集成测试
- [ ] 性能测试与优化

### 第三周：文档编写与代码审查
- [ ] 编写API文档
- [ ] 编写开发文档
- [ ] 编写用户文档
- [ ] 进行代码审查和优化

---

## 质量标准

### 代码质量
- [ ] 代码符合PEP 8规范
- [ ] 代码覆盖率达到85%以上
- [ ] 通过所有静态代码检查
- [ ] 通过代码审查

### 测试质量
- [ ] 单元测试覆盖率85%以上
- [ ] 核心功能测试覆盖率100%
- [ ] 集成测试通过
- [ ] 性能测试达标

### 文档质量
- [ ] API文档完整准确
- [ ] 开发文档清晰易懂
- [ ] 用户文档实用全面
- [ ] 示例代码可运行

---

## 风险评估与应对

### 技术风险
- **风险**：[描述技术风险]
- **影响**：[描述风险影响]
- **应对措施**：[描述应对措施]

### 进度风险
- **风险**：[描述进度风险]
- **影响**：[描述风险影响]
- **应对措施**：[描述应对措施]

### 质量风险
- **风险**：[描述质量风险]
- **影响**：[描述风险影响]
- **应对措施**：[描述应对措施]

---

## 交付物清单

### 代码交付物
- [ ] 源代码
- [ ] 单元测试代码
- [ ] 集成测试代码
- [ ] 配置文件
- [ ] 部署脚本

### 文档交付物
- [ ] API文档
- [ ] 开发文档
- [ ] 用户文档
- [ ] 测试报告
- [ ] 部署文档

---

## 总结

[模块名称]的实现工作流遵循"3C"模型，通过Code（代码实现）、Check（测试验证）和Document（文档编写）三个阶段，确保模块的高质量实现。在实施过程中，需要密切关注代码质量、测试覆盖率和文档完整性，确保模块功能符合设计要求，并为系统的成功交付奠定坚实基础。